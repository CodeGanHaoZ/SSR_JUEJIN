const o = "[@strapi/design-system]:", c = (t) => {
  const e = t;
  let r = !1;
  if (typeof e != "function")
    throw new TypeError(`${o} once requires a function parameter`);
  return (...n) => {
    r || (e(...n), r = !0);
  };
};
export {
  c as once,
  o as prefix
};
